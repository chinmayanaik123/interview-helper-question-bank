[
  {
    "id": "react-what-is",
    "category": "react",
    "difficulty": "beginner",
    "tags": ["basics", "library"],
    "question": "What is React and how is it different from a full framework like Angular?",
    "answer": "<p><strong>React</strong> is a JavaScript library for building UIs with a component model and declarative rendering. Facebook (Meta) open-sourced it; it focuses on the view layer.</p><p>Unlike <strong>Angular</strong> (a full framework with routing, DI, forms, HTTP, and a CLI opinionated structure), React is intentionally minimal. Routing, global state, and data fetching come from the ecosystem (React Router, Redux/Zustand, TanStack Query, Next.js).</p><ul><li><strong>React</strong> — library + ecosystem choices</li><li><strong>Angular</strong> — batteries-included framework</li></ul>",
    "tip": "Say 'library for UI' not 'framework' — interviewers notice. Then mention Next.js when full-stack React comes up.",
    "code": "",
    "lang": ""
  },
  {
    "id": "react-jsx",
    "category": "react",
    "difficulty": "beginner",
    "tags": ["jsx", "basics"],
    "question": "What is JSX and how does it work under the hood?",
    "answer": "<p><strong>JSX</strong> is a syntax extension that looks like HTML inside JavaScript. Build tools (Babel, SWC) transpile it to <code class=\"inline\">React.createElement</code> (or the automatic JSX runtime) calls that produce React elements — plain objects describing the UI.</p><p>JSX is not HTML: attributes use camelCase (<code class=\"inline\">className</code>, <code class=\"inline\">onClick</code>), expressions go in <code class=\"inline\">{ }</code>, and you return one parent (or a Fragment).</p>",
    "tip": "JSX = syntactic sugar for createElement. Elements are objects, not DOM nodes yet.",
    "code": "// JSX\nconst el = <h1 className=\"title\">Hello {name}</h1>;\n\n// Roughly compiles to\nReact.createElement('h1', { className: 'title' }, 'Hello ', name);",
    "lang": "javascript"
  },
  {
    "id": "react-virtual-dom",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["virtual-dom", "reconciliation", "V.Imp"],
    "question": "Explain the Virtual DOM and reconciliation.",
    "answer": "<p>The <strong>Virtual DOM</strong> is a lightweight in-memory tree of React elements. When state changes, React builds a new virtual tree, <strong>diffs</strong> it against the previous one (reconciliation), and applies the minimal set of real DOM updates.</p><p>This lets you write declarative UI (\"UI = f(state)\") while React batches and optimizes imperative DOM work. Keys help React match list items across updates so it reuses DOM nodes instead of remounting them.</p>",
    "tip": "Don't claim Virtual DOM is always faster than hand-tuned DOM — the win is predictable updates and developer productivity.",
    "code": "",
    "lang": "",
    "deep": "<h4>Pipeline</h4><ol><li>State/props change → component function re-runs</li><li>New element tree produced</li><li>Reconciler compares old vs new (Fiber)</li><li>Commit phase mutates the real DOM</li></ol><h4>Fiber (React 16+)</h4><p>Work is split into units that can pause/resume — enables concurrent features (interrupting low-priority updates for high-priority input).</p><h4>Keys matter</h4><p>Without stable keys, React may reuse the wrong DOM node (wrong input values, lost focus). Index-as-key is risky when lists reorder or filter.</p><h4>How to say it in an interview</h4><p>\"React keeps a virtual representation of the UI. On update it diffs the new tree against the old one and patches only what changed. Keys tell React which list items are the same identity across renders.\"</p>"
  },
  {
    "id": "react-components",
    "category": "react",
    "difficulty": "beginner",
    "tags": ["components", "basics"],
    "question": "Functional components vs class components — which should you use today?",
    "answer": "<p><strong>Functional components</strong> are plain functions that take props and return JSX. With Hooks they handle state, effects, and context — this is the modern default.</p><p><strong>Class components</strong> use <code class=\"inline\">extends React.Component</code>, lifecycle methods (<code class=\"inline\">componentDidMount</code>, etc.), and <code class=\"inline\">this.state</code>. Still work, but new code and docs focus on functions + Hooks.</p><p>Prefer functional components unless maintaining a legacy class codebase.</p>",
    "tip": "Map lifecycles to Hooks: DidMount/DidUpdate → useEffect; getDerivedStateFromProps is rarely needed.",
    "code": "function Greeting({ name }) {\n  return <h1>Hello, {name}</h1>;\n}",
    "lang": "javascript"
  },
  {
    "id": "react-props-vs-state",
    "category": "react",
    "difficulty": "beginner",
    "tags": ["props", "state", "basics", "V.Imp"],
    "question": "What is the difference between props and state?",
    "answer": "<p><strong>Props</strong> are inputs passed from a parent — read-only for the child. Changing props is the parent's job.</p><p><strong>State</strong> is data owned by the component. Updating state with a setter (e.g. <code class=\"inline\">setCount</code>) schedules a re-render.</p><ul><li>Props — configuration from outside</li><li>State — internal, mutable over time via setters</li></ul><p>Lift state up when siblings need to share it; pass data down as props.</p>",
    "tip": "Never mutate props. Never mutate state in place — always use the setter with a new value/object.",
    "code": "function Counter() {\n  const [count, setCount] = useState(0); // state\n  return <button onClick={() => setCount(c => c + 1)}>{count}</button>;\n}\n\nfunction App() {\n  return <Counter />; // no props here; parent could pass initialCount as a prop\n}",
    "lang": "javascript"
  },
  {
    "id": "react-usestate",
    "category": "react",
    "difficulty": "beginner",
    "tags": ["hooks", "useState"],
    "question": "How does useState work?",
    "answer": "<p><code class=\"inline\">useState(initial)</code> returns <code class=\"inline\">[value, setValue]</code>. Calling the setter queues an update and re-renders the component with the new value.</p><p>Use the <strong>functional updater</strong> <code class=\"inline\">setX(prev => ...)</code> when the next value depends on the previous one (especially in async callbacks or batched updates). Initial state can be a lazy function <code class=\"inline\">useState(() => expensive())</code> so it runs only once.</p>",
    "tip": "State updates are asynchronous/batched — reading state right after setState still shows the old value in the same tick.",
    "code": "const [user, setUser] = useState(null);\nsetUser(prev => ({ ...prev, name: 'Ada' }));",
    "lang": "javascript"
  },
  {
    "id": "react-useeffect",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["hooks", "useEffect", "V.Imp"],
    "question": "Explain useEffect — when does it run and what is the cleanup function for?",
    "answer": "<p><code class=\"inline\">useEffect(fn, deps)</code> runs <strong>after paint</strong> to synchronize with external systems (network, DOM APIs, subscriptions, timers).</p><ul><li><strong>No deps array</strong> — after every render</li><li><strong><code class=\"inline\">[]</code></strong> — once after mount (and cleanup on unmount)</li><li><strong><code class=\"inline\">[a, b]</code></strong> — when <code class=\"inline\">a</code> or <code class=\"inline\">b</code> change</li></ul><p>Return a <strong>cleanup</strong> function to unsubscribe, clear timers, or abort fetches — React runs it before the next effect and on unmount.</p>",
    "tip": "Effects are for side effects, not for deriving state — compute during render when you can.",
    "code": "useEffect(() => {\n  const id = setInterval(() => tick(), 1000);\n  return () => clearInterval(id);\n}, []);",
    "lang": "javascript",
    "deep": "<h4>Mental model</h4><p>\"Keep the outside world in sync with React state.\" Not a lifecycle replacement checklist.</p><h4>Strict Mode (dev)</h4><p>React 18+ may mount → cleanup → mount again in development to surface missing cleanups. Production runs once.</p><h4>Stale closures</h4><p>If the effect reads state/props but omits them from deps, it sees old values. Fix with correct deps, functional updaters, or refs for \"latest value without re-subscribing.\"</p><h4>Data fetching pattern</h4><pre><code>useEffect(() => {\n  const ac = new AbortController();\n  fetch(url, { signal: ac.signal })\n    .then(r => r.json())\n    .then(setData)\n    .catch(err => { if (err.name !== 'AbortError') setError(err); });\n  return () => ac.abort();\n}, [url]);</code></pre><h4>How to say it in an interview</h4><p>\"useEffect runs after paint to sync with external systems. The dependency array controls when it re-runs. I always return cleanup for subscriptions and abort in-flight requests on unmount or dep change.\"</p>"
  },
  {
    "id": "react-hooks-rules",
    "category": "react",
    "difficulty": "beginner",
    "tags": ["hooks", "rules"],
    "question": "What are the Rules of Hooks?",
    "answer": "<p>Two rules:</p><ol><li><strong>Only call Hooks at the top level</strong> — not inside loops, conditions, or nested functions. Order must be stable across renders.</li><li><strong>Only call Hooks from React functions</strong> — function components or custom Hooks (names starting with <code class=\"inline\">use</code>).</li></ol><p>Violating them breaks React's internal Hook linked list (state from the wrong Hook).</p>",
    "tip": "eslint-plugin-react-hooks catches most mistakes — keep it on in every project.",
    "code": "// BAD\nif (cond) {\n  const [x, setX] = useState(0);\n}\n\n// GOOD — always call, branch inside\nconst [x, setX] = useState(0);\nif (cond) { /* use x */ }",
    "lang": "javascript"
  },
  {
    "id": "react-useref",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["hooks", "useRef"],
    "question": "When do you use useRef?",
    "answer": "<p><code class=\"inline\">useRef(initial)</code> returns a mutable object <code class=\"inline\">{ current: ... }</code> that <strong>persists across renders</strong> and updating it <strong>does not trigger a re-render</strong>.</p><p>Common uses:</p><ul><li>Hold a DOM node: <code class=\"inline\">ref={inputRef}</code> then <code class=\"inline\">inputRef.current.focus()</code></li><li>Store previous values, timer IDs, or latest callback without re-subscribing effects</li></ul>",
    "tip": "Refs for values that change but shouldn't cause UI updates; state for values that should.",
    "code": "const inputRef = useRef(null);\n\nfunction focus() {\n  inputRef.current?.focus();\n}\n\nreturn <input ref={inputRef} />;",
    "lang": "javascript"
  },
  {
    "id": "react-usememo-usecallback",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["hooks", "performance", "useMemo", "useCallback"],
    "question": "useMemo vs useCallback — when are they worth using?",
    "answer": "<p><strong>useMemo(() => value, deps)</strong> caches a computed value between renders.</p><p><strong>useCallback(fn, deps)</strong> caches a function reference (sugar for <code class=\"inline\">useMemo(() => fn, deps)</code>).</p><p>Use them when:</p><ul><li>Passing callbacks/objects to memoized children (<code class=\"inline\">React.memo</code>) to avoid pointless re-renders</li><li>Expensive calculations</li><li>Stable deps for effects that shouldn't re-run every render</li></ul><p>Don't wrap everything — premature memoization adds complexity and can be slower for cheap work.</p>",
    "tip": "Profile first. Memo without React.memo children often does nothing useful.",
    "code": "const sorted = useMemo(() => [...items].sort(cmp), [items]);\nconst onSelect = useCallback(id => setSelected(id), []);",
    "lang": "javascript"
  },
  {
    "id": "react-usecontext",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["hooks", "context", "state"],
    "question": "How does Context API work and when should you use it?",
    "answer": "<p><code class=\"inline\">createContext</code> + <code class=\"inline\">Provider</code> + <code class=\"inline\">useContext</code> let you pass data through the tree without prop drilling.</p><p>Good for: theme, auth user, locale, feature flags — relatively stable, widely needed values.</p><p>Weak for: high-frequency updates (every keystroke) across a large tree — all consumers re-render. Split contexts, memoize provider value, or use a dedicated state library for complex client state.</p>",
    "tip": "Always memoize the Provider value object/function or you re-render all consumers every parent render.",
    "code": "const ThemeContext = createContext('light');\n\nfunction App() {\n  const [theme, setTheme] = useState('dark');\n  const value = useMemo(() => ({ theme, setTheme }), [theme]);\n  return (\n    <ThemeContext.Provider value={value}>\n      <Page />\n    </ThemeContext.Provider>\n  );\n}\n\nfunction Page() {\n  const { theme } = useContext(ThemeContext);\n  return <div className={theme}>...</div>;\n}",
    "lang": "javascript"
  },
  {
    "id": "react-custom-hooks",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["hooks", "custom-hooks", "reuse"],
    "question": "What are custom Hooks and why write them?",
    "answer": "<p>A <strong>custom Hook</strong> is a function named <code class=\"inline\">useSomething</code> that calls other Hooks. It extracts reusable stateful logic without forcing a shared component tree (unlike HOCs).</p><p>Examples: <code class=\"inline\">useFetch</code>, <code class=\"inline\">useLocalStorage</code>, <code class=\"inline\">useDebounce</code>, <code class=\"inline\">useMediaQuery</code>.</p><p>Each component that calls the custom Hook gets its <em>own</em> state — Hooks don't share state by calling the same function; they share the pattern.</p>",
    "tip": "If two components need the same state instance, lift state or use Context — not a shared custom Hook alone.",
    "code": "function useToggle(initial = false) {\n  const [on, setOn] = useState(initial);\n  const toggle = useCallback(() => setOn(v => !v), []);\n  return [on, toggle];\n}",
    "lang": "javascript"
  },
  {
    "id": "react-keys",
    "category": "react",
    "difficulty": "beginner",
    "tags": ["lists", "keys", "V.Imp"],
    "question": "Why do lists need keys, and why is using the array index often a bad idea?",
    "answer": "<p><strong>Keys</strong> help React identify which items changed, were added, or removed during reconciliation.</p><p>Stable, unique IDs from your data are best. <strong>Index as key</strong> breaks when the list is reordered, filtered, or items inserted at the front — React reuses the wrong component instance (wrong local state, focus bugs).</p><p>Index keys are acceptable only for static lists that never reorder or grow/shrink in the middle.</p>",
    "tip": "Never use Math.random() as a key — remounts every render.",
    "code": "items.map(item => (\n  <TodoItem key={item.id} todo={item} />\n));",
    "lang": "javascript"
  },
  {
    "id": "react-controlled-uncontrolled",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["forms", "controlled", "uncontrolled"],
    "question": "Controlled vs uncontrolled components in forms.",
    "answer": "<p><strong>Controlled</strong> — form value lives in React state; the input's <code class=\"inline\">value</code> + <code class=\"inline\">onChange</code> keep the DOM in sync. Full control for validation, disable submit, format-as-you-type.</p><p><strong>Uncontrolled</strong> — the DOM owns the value; you read it via a ref (or FormData) on submit. Less re-renders; fine for simple forms.</p><p>Libraries like React Hook Form lean uncontrolled under the hood for performance, with controlled APIs when needed.</p>",
    "tip": "If you set value={x} you must also update x on change — otherwise the input appears frozen.",
    "code": "// Controlled\nconst [name, setName] = useState('');\n<input value={name} onChange={e => setName(e.target.value)} />;\n\n// Uncontrolled\nconst ref = useRef();\n<input ref={ref} defaultValue=\"Ada\" />;",
    "lang": "javascript"
  },
  {
    "id": "react-lifting-state",
    "category": "react",
    "difficulty": "beginner",
    "tags": ["state", "architecture"],
    "question": "What does \"lifting state up\" mean?",
    "answer": "<p>When two sibling components need the same data, move that state to their closest common parent and pass it down as props (and pass setters/callbacks to update it).</p><p>This keeps a single source of truth and avoids duplicated or out-of-sync state. If the tree gets deep, consider Context or a state library instead of prop-drilling through many layers.</p>",
    "tip": "State should live in the nearest component that needs to coordinate its consumers.",
    "code": "",
    "lang": ""
  },
  {
    "id": "react-prop-drilling",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["props", "context", "architecture"],
    "question": "What is prop drilling and how do you avoid it?",
    "answer": "<p><strong>Prop drilling</strong> is passing props through many intermediate components that don't use them, only to reach a deep child.</p><p>Mitigations:</p><ul><li>Component composition (children / slots)</li><li>Context for cross-cutting data</li><li>Colocate state closer to where it's used</li><li>State libraries (Zustand, Redux) for complex app state</li></ul>",
    "tip": "Don't jump to Redux for every drill — composition and Context often fix it with less ceremony.",
    "code": "",
    "lang": ""
  },
  {
    "id": "react-memo",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["performance", "React.memo"],
    "question": "What does React.memo do?",
    "answer": "<p><code class=\"inline\">React.memo(Component)</code> wraps a component so it <strong>skips re-render</strong> if props are shallowly equal to the previous props.</p><p>Useful for expensive pure children when the parent re-renders often. Combine with <code class=\"inline\">useCallback</code>/<code class=\"inline\">useMemo</code> so prop references stay stable. Custom comparison via the second argument is possible but easy to get wrong.</p>",
    "tip": "memo is not free — skip it until you measure a real re-render problem.",
    "code": "const Row = React.memo(function Row({ item }) {\n  return <div>{item.name}</div>;\n});",
    "lang": "javascript"
  },
  {
    "id": "react-usereducer",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["hooks", "useReducer", "state"],
    "question": "When would you choose useReducer over useState?",
    "answer": "<p><code class=\"inline\">useReducer(reducer, initialState)</code> centralizes complex state transitions: next state = <code class=\"inline\">reducer(state, action)</code>.</p><p>Prefer it when:</p><ul><li>Multiple related fields update together</li><li>Next state depends on previous in non-trivial ways</li><li>You want testable, named actions (like a mini Redux)</li></ul><p>For a single boolean or string, <code class=\"inline\">useState</code> is clearer.</p>",
    "tip": "useReducer + Context is a lightweight global store pattern without Redux.",
    "code": "function reducer(state, action) {\n  switch (action.type) {\n    case 'increment': return { count: state.count + 1 };\n    case 'reset': return { count: 0 };\n    default: return state;\n  }\n}\nconst [state, dispatch] = useReducer(reducer, { count: 0 });",
    "lang": "javascript"
  },
  {
    "id": "react-error-boundaries",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["errors", "error-boundaries"],
    "question": "What are Error Boundaries?",
    "answer": "<p><strong>Error Boundaries</strong> are React components that catch JavaScript errors in their child tree during render/lifecycle, log them, and show a fallback UI instead of crashing the whole app.</p><p>They must be class components (or a library wrapper) implementing <code class=\"inline\">getDerivedStateFromError</code> / <code class=\"inline\">componentDidCatch</code>. They do <strong>not</strong> catch errors in event handlers, async code, or SSR — use try/catch there.</p>",
    "tip": "Place boundaries around route-level or widget-level chunks so one failing widget doesn't blank the page.",
    "code": "class ErrorBoundary extends React.Component {\n  state = { hasError: false };\n  static getDerivedStateFromError() {\n    return { hasError: true };\n  }\n  render() {\n    if (this.state.hasError) return <h2>Something went wrong.</h2>;\n    return this.props.children;\n  }\n}",
    "lang": "javascript"
  },
  {
    "id": "react-lazy-suspense",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["code-splitting", "lazy", "Suspense", "performance"],
    "question": "How do React.lazy and Suspense help with code splitting?",
    "answer": "<p><code class=\"inline\">React.lazy(() => import('./Page'))</code> loads a component via dynamic <code class=\"inline\">import()</code>, creating a separate bundle chunk.</p><p>Wrap lazy components in <code class=\"inline\">&lt;Suspense fallback={...}&gt;</code> to show a loading UI while the chunk downloads. Common for route-based splitting (with React Router).</p>",
    "tip": "Split by route first — biggest win with least complexity.",
    "code": "const Settings = React.lazy(() => import('./Settings'));\n\n<Suspense fallback={<Spinner />}>\n  <Settings />\n</Suspense>",
    "lang": "javascript"
  },
  {
    "id": "react-router",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["routing", "react-router"],
    "question": "How does client-side routing work with React Router?",
    "answer": "<p><strong>React Router</strong> maps URL paths to components without full page reloads. The browser History API updates the URL; React swaps the matched route element.</p><p>Core ideas: <code class=\"inline\">BrowserRouter</code>, route config / <code class=\"inline\">Routes</code>+<code class=\"inline\">Route</code>, nested routes with <code class=\"inline\">Outlet</code>, <code class=\"inline\">Link</code>/<code class=\"inline\">NavLink</code>, loaders/actions (data APIs in v6.4+), and protected routes via layout routes or loaders that check auth.</p>",
    "tip": "Know nested routes and Outlet — that's the modern React Router interview focus.",
    "code": "import { Routes, Route, Outlet } from 'react-router-dom';\n\nfunction Layout() {\n  return (\n    <>\n      <Nav />\n      <Outlet />\n    </>\n  );\n}\n\n<Routes>\n  <Route element={<Layout />}>\n    <Route path=\"/\" element={<Home />} />\n    <Route path=\"users/:id\" element={<User />} />\n  </Route>\n</Routes>",
    "lang": "javascript"
  },
  {
    "id": "react-state-management",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["redux", "zustand", "state", "V.Imp"],
    "question": "How do you choose between Context, Redux, and Zustand for state?",
    "answer": "<p><strong>Local useState/useReducer</strong> — UI-only, one component or small subtree.</p><p><strong>Context</strong> — low-frequency shared data (theme, auth). Avoid for chatty updates.</p><p><strong>Redux Toolkit</strong> — large apps needing predictable updates, middleware, DevTools, normalized entities, time-travel debugging.</p><p><strong>Zustand / Jotai / Recoil</strong> — lighter stores with less boilerplate; great default for many SPAs.</p><p>Also separate <strong>server state</strong> (TanStack Query / SWR) from client UI state — don't put API cache in Redux by default.</p>",
    "tip": "Interview answer: start local → Context → library when complexity or performance demands it. Mention server-state libraries.",
    "code": "",
    "lang": "",
    "deep": "<h4>Decision cheatsheet</h4><table><thead><tr><th>Need</th><th>Tool</th></tr></thead><tbody><tr><td>Form open/closed</td><td>useState</td></tr><tr><td>Theme / current user</td><td>Context</td></tr><tr><td>Complex client workflows</td><td>Zustand or Redux Toolkit</td></tr><tr><td>REST/GraphQL cache, retries</td><td>TanStack Query</td></tr></tbody></table><h4>Redux today</h4><p>Use Redux Toolkit (<code class=\"inline\">createSlice</code>, <code class=\"inline\">configureStore</code>) — classic boilerplate Redux is outdated in interviews.</p><h4>How to say it</h4><p>\"I keep server cache in React Query. Local UI state stays in components. For shared client state I prefer Zustand unless the team already standardized on Redux Toolkit.\"</p>"
  },
  {
    "id": "react-synthetic-events",
    "category": "react",
    "difficulty": "beginner",
    "tags": ["events", "basics"],
    "question": "What are Synthetic Events in React?",
    "answer": "<p>React wraps native browser events in a <strong>SyntheticEvent</strong> for cross-browser consistency. You attach handlers with camelCase props (<code class=\"inline\">onClick</code>, <code class=\"inline\">onChange</code>).</p><p>In modern React, pooling is largely gone — you can use the event asynchronously. Still call <code class=\"inline\">e.preventDefault()</code> / <code class=\"inline\">e.stopPropagation()</code> as with native events. React delegates many events at the root for performance.</p>",
    "tip": "Pass the function reference: onClick={handler}, not onClick={handler()} unless you intentionally call it.",
    "code": "function Button() {\n  function handleClick(e) {\n    e.preventDefault();\n    console.log(e.type);\n  }\n  return <button onClick={handleClick}>Save</button>;\n}",
    "lang": "javascript"
  },
  {
    "id": "react-fragments-portals",
    "category": "react",
    "difficulty": "beginner",
    "tags": ["fragments", "portals"],
    "question": "What are Fragments and Portals?",
    "answer": "<p><strong>Fragments</strong> (<code class=\"inline\">&lt;&gt;...&lt;/&gt;</code> or <code class=\"inline\">&lt;React.Fragment&gt;</code>) group children without an extra DOM node. Useful when a component must return multiple siblings.</p><p><strong>Portals</strong> (<code class=\"inline\">createPortal(child, domNode)</code>) render children into a DOM node outside the parent hierarchy (modals, tooltips, toasts) while preserving React context and event bubbling in the React tree.</p>",
    "tip": "Modals almost always use portals so z-index/overflow from parents don't clip them.",
    "code": "import { createPortal } from 'react-dom';\n\ncreatePortal(<Modal />, document.getElementById('modal-root'));",
    "lang": "javascript"
  },
  {
    "id": "react-strict-mode",
    "category": "react",
    "difficulty": "beginner",
    "tags": ["strict-mode", "dev"],
    "question": "What does React Strict Mode do?",
    "answer": "<p><code class=\"inline\">&lt;StrictMode&gt;</code> is a development-only helper that highlights unsafe patterns. In React 18+ it double-invokes certain functions (render, effects setup/cleanup) to surface missing effect cleanups and impure renders.</p><p>It does not affect production builds. Seeing effects run twice in dev is usually Strict Mode — not a production bug by itself.</p>",
    "tip": "If an effect breaks under Strict Mode double-mount, your cleanup is incomplete.",
    "code": "import { StrictMode } from 'react';\n\nroot.render(\n  <StrictMode>\n    <App />\n  </StrictMode>\n);",
    "lang": "javascript"
  },
  {
    "id": "react-uselayout-effect",
    "category": "react",
    "difficulty": "advanced",
    "tags": ["hooks", "useLayoutEffect"],
    "question": "useEffect vs useLayoutEffect — when do you need useLayoutEffect?",
    "answer": "<p>Both run after React updates the DOM in memory, but:</p><ul><li><strong>useEffect</strong> — after the browser paints (async). Default choice.</li><li><strong>useLayoutEffect</strong> — synchronously after DOM mutations, <em>before</em> paint. Use when you must measure layout (<code class=\"inline\">getBoundingClientRect</code>) or prevent a visible flicker by applying DOM updates before the user sees the frame.</li></ul><p>Overusing useLayoutEffect blocks painting and hurts performance — prefer useEffect unless you see visual glitches.</p>",
    "tip": "Tooltip positioning and scroll restoration are classic useLayoutEffect cases.",
    "code": "useLayoutEffect(() => {\n  const { height } = ref.current.getBoundingClientRect();\n  setHeight(height);\n}, [content]);",
    "lang": "javascript"
  },
  {
    "id": "react-concurrent-transition",
    "category": "react",
    "difficulty": "advanced",
    "tags": ["concurrent", "useTransition", "performance"],
    "question": "What are concurrent features and useTransition?",
    "answer": "<p><strong>Concurrent rendering</strong> (React 18+) lets React interrupt, prioritize, and reuse work so the UI stays responsive.</p><p><code class=\"inline\">useTransition</code> marks state updates as <strong>non-urgent</strong>. Urgent updates (typing in an input) stay snappy while a heavy filtered list update can lag without blocking input.</p><p><code class=\"inline\">useDeferredValue</code> defers re-rendering with a lagging value of a prop/state for similar goals.</p>",
    "tip": "Pair transitions with Suspense for smoother loading of slow children.",
    "code": "const [isPending, startTransition] = useTransition();\nconst [query, setQuery] = useState('');\nconst [list, setList] = useState(items);\n\nfunction onChange(e) {\n  setQuery(e.target.value); // urgent\n  startTransition(() => {\n    setList(filterHuge(e.target.value)); // non-urgent\n  });\n}",
    "lang": "javascript"
  },
  {
    "id": "react-hoc-vs-hooks",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["hoc", "hooks", "patterns"],
    "question": "HOCs and render props vs Hooks — what changed?",
    "answer": "<p><strong>HOCs</strong> (<code class=\"inline\">withAuth(Component)</code>) and <strong>render props</strong> (<code class=\"inline\">&lt;Data render={data => ...} /&gt;</code>) were classic ways to reuse logic. They nest wrappers (\"wrapper hell\") and obscure the component tree.</p><p><strong>Hooks</strong> extract the same logic into composable functions without changing the component hierarchy. Hooks are the preferred pattern for new code; you may still see HOCs in older codebases or libraries.</p>",
    "tip": "If asked to refactor a HOC to Hooks, extract the shared logic into useX and call it inside the component.",
    "code": "",
    "lang": ""
  },
  {
    "id": "react-conditional-rendering",
    "category": "react",
    "difficulty": "beginner",
    "tags": ["rendering", "basics"],
    "question": "What are common patterns for conditional rendering in React?",
    "answer": "<p>Common patterns:</p><ul><li><code class=\"inline\">{cond && &lt;Child /&gt;}</code> — render when true (watch out for <code class=\"inline\">0</code> rendering)</li><li>Ternary: <code class=\"inline\">{cond ? &lt;A /&gt; : &lt;B /&gt;}</code></li><li>Early return: <code class=\"inline\">if (!data) return &lt;Spinner /&gt;;</code></li><li>Switch / lookup maps for multiple views</li></ul><p>Prefer clear early returns over deeply nested ternaries.</p>",
    "tip": "Use Boolean(cond) && or cond ? ... : null when cond might be 0.",
    "code": "if (loading) return <Spinner />;\nif (error) return <Error msg={error} />;\nreturn <Profile user={user} />;",
    "lang": "javascript"
  },
  {
    "id": "react-pure-impure",
    "category": "react",
    "difficulty": "advanced",
    "tags": ["rendering", "purity", "best-practices"],
    "question": "Why should render be pure, and what belongs in effects?",
    "answer": "<p>Component render should be a <strong>pure function</strong> of props/state: same inputs → same JSX, no side effects. Don't fetch, write localStorage, or mutate shared objects during render.</p><p>Side effects belong in <strong>event handlers</strong> (user-triggered) or <strong>useEffect</strong> (synchronize with external systems). Impure renders cause Strict Mode bugs, broken concurrent rendering, and hard-to-reproduce issues.</p>",
    "tip": "If you need to adjust state based on props, usually derive it during render or use a key to reset state — avoid copying props into state unnecessarily.",
    "code": "",
    "lang": ""
  },
  {
    "id": "react-server-components",
    "category": "react",
    "difficulty": "advanced",
    "tags": ["rsc", "nextjs", "server"],
    "question": "What are React Server Components (RSC) at a high level?",
    "answer": "<p><strong>Server Components</strong> render on the server and send a serialized UI result to the client. They can access databases/files directly and ship zero (or less) JS for that subtree.</p><p><strong>Client Components</strong> (<code class=\"inline\">'use client'</code>) run in the browser and can use state, effects, and event handlers.</p><p>Frameworks like <strong>Next.js App Router</strong> popularized RSC. Default to Server Components; push interactivity to Client Component leaves.</p>",
    "tip": "You can't use useState/useEffect in a Server Component — that's the quick mental check.",
    "code": "",
    "lang": "",
    "deep": "<h4>Why RSCs exist</h4><ul><li>Smaller client bundles</li><li>Direct server data access without extra API layer for that view</li><li>Keep secrets on the server</li></ul><h4>Composition</h4><p>Server Components can import Client Components, but not vice versa for server-only modules. Pass Server Components as children into Client wrappers when needed.</p><h4>How to say it</h4><p>\"Server Components render on the server and reduce client JS. Client Components handle interactivity. In Next.js App Router I keep pages/server-fetching as Server Components and mark interactive widgets with use client.\"</p>"
  },
  {
    "id": "react-testing",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["testing", "rtl"],
    "question": "How do you typically test React components?",
    "answer": "<p>Prefer <strong>React Testing Library (RTL)</strong> with Jest/Vitest: render the component, query by role/label/text (how users see the UI), fire events, assert outcomes.</p><p>Avoid testing implementation details (internal state, CSS classes) when possible. Use MSW for API mocking. For E2E, Playwright or Cypress cover full flows.</p>",
    "tip": "Query priority: getByRole → getByLabelText → getByText. Avoid data-testid unless necessary.",
    "code": "import { render, screen } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\n\ntest('increments', async () => {\n  render(<Counter />);\n  await userEvent.click(screen.getByRole('button', { name: /add/i }));\n  expect(screen.getByText('1')).toBeInTheDocument();\n});",
    "lang": "javascript"
  },
  {
    "id": "react-performance-checklist",
    "category": "react",
    "difficulty": "advanced",
    "tags": ["performance", "V.Imp"],
    "question": "How do you diagnose and fix React performance issues?",
    "answer": "<p>Approach:</p><ol><li><strong>Measure</strong> — React Profiler, Chrome Performance, why-did-you-render</li><li><strong>Cut unnecessary re-renders</strong> — state closer to consumers, memo children, stable callbacks</li><li><strong>Cut expensive work</strong> — useMemo for heavy calc, virtualize long lists (react-window / tanstack-virtual)</li><li><strong>Cut bundle size</strong> — lazy routes, tree-shakeable imports</li><li><strong>Concurrent tools</strong> — useTransition for heavy UI updates</li></ol><p>Don't memoize blindly — fix state structure and list keys first.</p>",
    "tip": "The #1 real-world fix is often: fewer components subscribed to a huge Context, or virtualizing a giant table.",
    "code": "",
    "lang": ""
  },
  {
    "id": "react-composition",
    "category": "react",
    "difficulty": "intermediate",
    "tags": ["composition", "children", "patterns"],
    "question": "Why is composition preferred over inheritance in React?",
    "answer": "<p>React favors <strong>composition</strong>: nest components, pass <code class=\"inline\">children</code>, or use named slots/props that accept JSX. You reuse UI and behavior by combining pieces, not by extending class hierarchies.</p><p>Inheritance couples components and fights the function-component model. Patterns like compound components (<code class=\"inline\">Select</code> + <code class=\"inline\">Select.Option</code>) and children-as-API keep APIs flexible.</p>",
    "tip": "If you reach for extends, stop — extract a Hook or wrap with children instead.",
    "code": "function Card({ title, children, actions }) {\n  return (\n    <section>\n      <h2>{title}</h2>\n      <div>{children}</div>\n      <footer>{actions}</footer>\n    </section>\n  );\n}",
    "lang": "javascript"
  }
]
