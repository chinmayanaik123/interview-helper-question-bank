[
  {
    "id": "ts-interface-type",
    "category": "typescript",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "types",
      "interface",
      "type-alias"
    ],
    "question": "interface vs type — what's the difference?",
    "answer": "<p>Both describe shapes. <strong>interface</strong> can be re-opened/merged (declaration merging) and is the idiom for object/class contracts. <strong>type</strong> is more flexible — it can be a union, intersection, tuple, or alias a primitive.</p><p>Rule of thumb: <code class=\"inline\">interface</code> for public object shapes, <code class=\"inline\">type</code> for unions and utility compositions.</p>",
    "tip": "Only type can express a union like 'a' | 'b'.",
    "code": "type Status = 'active' | 'inactive';\ninterface User { id: number; name: string; }",
    "lang": "ts",
    "deep": "<h4>What only <code class=\"inline\">type</code> can do</h4><pre><code>type Status  = 'idle' | 'loading' | 'done';   // union      -- interface can't\ntype Pair    = [string, number];              // tuple      -- interface can't\ntype Fn      = (a: number) =&gt; string;         // (interface can, awkwardly)\ntype Keys    = keyof User;                    // computed   -- interface can't\ntype Partial2&lt;T&gt; = { [K in keyof T]?: T[K] }; // mapped     -- interface can't</code></pre><h4>What only <code class=\"inline\">interface</code> can do: declaration merging</h4><pre><code>interface Window { myApp: App; }   // merges into the built-in Window\ninterface Window { debug: boolean; } // both now exist\n\ntype A = { x: number };\ntype A = { y: number };   // ✘ Error: Duplicate identifier 'A'</code></pre><p>This is why <strong>library augmentation</strong> uses interfaces — it's how you add typing to <code class=\"inline\">window</code>, extend Express's <code class=\"inline\">Request</code>, or add a property to a third-party type. It's a feature, not a quirk.</p><h4>Side by side</h4><table><thead><tr><th></th><th>interface</th><th>type</th></tr></thead><tbody><tr><td>Object/class shape</td><td>✔</td><td>✔</td></tr><tr><td>Union</td><td>✘</td><td>✔</td></tr><tr><td>Tuple</td><td>✘</td><td>✔</td></tr><tr><td>Mapped / conditional</td><td>✘</td><td>✔</td></tr><tr><td>Declaration merging</td><td>✔</td><td>✘</td></tr><tr><td><code class=\"inline\">implements</code></td><td>✔</td><td>✔ (if it's an object type)</td></tr><tr><td>Extends</td><td><code class=\"inline\">extends</code></td><td><code class=\"inline\">&amp;</code> intersection</td></tr></tbody></table><h4>The subtle difference: extends vs &amp;</h4><pre><code>interface A { x: string }\ninterface B extends A { x: number }   // ✘ ERROR — caught: incompatible override\n\ntype A2 = { x: string };\ntype B2 = A2 &amp; { x: number };         // ✔ compiles... x is now `never`!</code></pre><p><code class=\"inline\">extends</code> <strong>checks</strong> compatibility and errors on conflict; <code class=\"inline\">&amp;</code> silently intersects, and conflicting primitives collapse to <code class=\"inline\">never</code> — a bug you find much later. That's a genuine argument for interfaces in class hierarchies.</p><h4>Performance (the answer almost nobody gives)</h4><p>Interfaces are cached by name; intersections are recomputed structurally. In a large codebase, deep chains of <code class=\"inline\">&amp;</code> measurably slow the compiler. The TypeScript team's own guidance is to prefer <code class=\"inline\">interface</code> until you need a feature only <code class=\"inline\">type</code> has.</p><h4>The practical rule</h4><ul><li><strong>Object shape or class contract</strong> → <code class=\"inline\">interface</code> (extensible, better errors, faster).</li><li><strong>Union, tuple, mapped, conditional, or a function alias</strong> → <code class=\"inline\">type</code> (no choice).</li><li>Be consistent within a codebase — the churn costs more than the choice.</li></ul><h4>How to say it in an interview</h4><p>“They overlap for object shapes. Only <code class=\"inline\">type</code> can do unions, tuples, mapped and conditional types; only <code class=\"inline\">interface</code> can declaration-merge, which is how you augment <code class=\"inline\">Window</code> or a library's types. The subtle one is that <code class=\"inline\">extends</code> errors on a conflicting property while an intersection silently gives you <code class=\"inline\">never</code>. I default to <code class=\"inline\">interface</code> for shapes — it's also faster for the compiler — and reach for <code class=\"inline\">type</code> when I need what only it can express.”</p>"
  },
  {
    "id": "ts-any-unknown-never",
    "category": "typescript",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "types",
      "any",
      "unknown",
      "never"
    ],
    "question": "any vs unknown vs never.",
    "answer": "<ul><li><strong>any</strong> — turns off type checking (avoid).</li><li><strong>unknown</strong> — a safe any; you must narrow it before use.</li><li><strong>never</strong> — a value that can't exist (a function that always throws, or an exhausted union).</li></ul>",
    "tip": "Prefer unknown over any for untyped input like JSON.parse results.",
    "code": "// any — checking OFF, everything allowed, crashes at runtime\nconst a: any = JSON.parse(s);\na.foo.bar.baz();          // compiles. explodes.\n\n// unknown — must narrow before use\nconst u: unknown = JSON.parse(s);\nif (typeof u === 'object' && u !== null && 'name' in u) {\n  console.log((u as { name: string }).name);   // now safe\n}\n\n// never — no value can exist\nfunction fail(msg: string): never { throw new Error(msg); }",
    "lang": "ts",
    "deep": "<h4>The mental model</h4><table><thead><tr><th></th><th><code class=\"inline\">any</code></th><th><code class=\"inline\">unknown</code></th><th><code class=\"inline\">never</code></th></tr></thead><tbody><tr><td>Meaning</td><td>\"stop checking\"</td><td>\"could be anything\"</td><td>\"impossible\"</td></tr><tr><td>Assign <em>to</em> it</td><td>anything</td><td>anything</td><td><strong>nothing</strong></td></tr><tr><td>Assign it <em>elsewhere</em></td><td>anywhere</td><td><strong>only to unknown/any</strong></td><td>anywhere</td></tr><tr><td>Use without narrowing</td><td>✔ (unsafe)</td><td><strong>✘</strong></td><td>n/a</td></tr></tbody></table><p><code class=\"inline\">unknown</code> is the <strong>top</strong> type (everything is assignable to it); <code class=\"inline\">never</code> is the <strong>bottom</strong> type (assignable to everything, but nothing is assignable to it). <code class=\"inline\">any</code> isn't a type so much as an <em>escape hatch</em> — it opts out of the system in both directions.</p><h4>Why any is dangerous: it's contagious</h4><pre><code>const data: any = await res.json();\nconst user = data.user;        // user is any\nconst name = user.name;        // name is any\nrenderTitle(name);             // no check at the call site either</code></pre><p>One <code class=\"inline\">any</code> silently spreads through every downstream inference, disabling checking in code that <em>looks</em> typed. That's why <code class=\"inline\">noImplicitAny</code> exists and why reviewers treat <code class=\"inline\">any</code> as a smell.</p><h4>unknown forces the check</h4><pre><code>function handle(e: unknown) {\n  // e.message                      ✘ 'e' is of type 'unknown'\n  if (e instanceof Error) e.message;   // ✔ narrowed\n}</code></pre><p>Since TS 4.4, <code class=\"inline\">catch (e)</code> is <code class=\"inline\">unknown</code> by default (<code class=\"inline\">useUnknownInCatchVariables</code>) — because you genuinely can't know what was thrown; JS lets you <code class=\"inline\">throw 42</code>.</p><h4>never — three places you actually meet it</h4><pre><code>// 1. a function that never returns normally\nfunction fail(m: string): never { throw new Error(m); }\n\n// 2. exhaustiveness checking ← the useful one\nfunction area(s: Shape): number {\n  switch (s.kind) {\n    case 'circle': return Math.PI * s.r ** 2;\n    case 'square': return s.size ** 2;\n    default:\n      const _exhaustive: never = s;   // ✘ compile error if a variant is unhandled\n      return _exhaustive;\n  }\n}\n\n// 3. an impossible intersection\ntype Impossible = string &amp; number;   // never</code></pre><p>Use #2. Add a <code class=\"inline\">Triangle</code> to the union and the compiler points straight at the switch you forgot to update — that's a compile-time guarantee no test suite gives you.</p><h4>The array trap</h4><pre><code>const arr = [];          // implicitly any[] (or never[] under strict)\narr.push(1);             // under strict: ✘ not assignable to never\nconst arr2: string[] = []; // annotate it</code></pre><h4>How to say it in an interview</h4><p>“<code class=\"inline\">any</code> turns checking off and it's contagious — one <code class=\"inline\">any</code> silently spreads through everything it touches. <code class=\"inline\">unknown</code> is the safe version: you can assign anything <em>to</em> it, but you must narrow before you use it, which is why <code class=\"inline\">catch</code> variables are <code class=\"inline\">unknown</code> now. <code class=\"inline\">never</code> is the bottom type — nothing is assignable to it, and I use that deliberately for exhaustiveness: assigning the switch variable to <code class=\"inline\">never</code> in the default case makes the compiler fail if someone adds a union member and forgets a branch.”</p>"
  },
  {
    "id": "ts-generics",
    "category": "typescript",
    "difficulty": "intermediate",
    "tags": [
      "generics"
    ],
    "question": "What are generics?",
    "answer": "<p><strong>Generics</strong> allow functions, classes, interfaces, and types to work with different data types while preserving type safety. Instead of using <code class=\"inline\">any</code>, a generic acts as a placeholder type that is determined when the function or class is used.</p>",
    "tip": "Think of generics as <strong>'type parameters'</strong>. Use <code class=\"inline\">extends</code> to restrict what types are allowed.",
    "code": "// Generic Function\nfunction first<T>(arr: T[]): T {\n  return arr[0];\n}\n\nconst name = first(['Alice', 'Bob']);\nconst number = first([10, 20, 30]);\n\nconsole.log(name);   // Alice\nconsole.log(number); // 10\n\n// Generic Interface\ninterface ApiResponse<T> {\n  data: T;\n  success: boolean;\n}\n\nconst response: ApiResponse<string[]> = {\n  data: ['Angular', 'React'],\n  success: true\n};",
    "lang": "ts",
    "deep": "<h4>What are Generics?</h4><p>Generics allow us to write reusable code that works with multiple data types while maintaining compile-time type safety. Instead of writing separate functions for strings, numbers, or custom objects, we write one generic implementation.</p><pre><code>// Without Generics\nfunction first(arr: any[]) {\n  return arr[0];\n}</code></pre><p>The above function works, but the return type becomes <code class=\"inline\">any</code>, so TypeScript loses type safety.</p><pre><code>// With Generics\nfunction first&lt;T&gt;(arr: T[]): T {\n  return arr[0];\n}</code></pre><p>The type <code class=\"inline\">T</code> is inferred from the argument passed to the function.</p><h4>Type Inference</h4><p>Most of the time, TypeScript automatically infers the generic type.</p><pre><code>const name = first(['John', 'Alice']);\n// T = string\n\nconst age = first([20, 30]);\n// T = number</code></pre><p>You can also specify it explicitly.</p><pre><code>first&lt;string&gt;(['A', 'B']);</code></pre><h4>Generic Interfaces</h4><p>Generics are commonly used with interfaces to model reusable data structures.</p><pre><code>interface ApiResponse&lt;T&gt; {\n  data: T;\n  success: boolean;\n}\n\nconst users: ApiResponse&lt;string[]&gt; = {\n  data: ['John', 'Alice'],\n  success: true\n};</code></pre><h4>Generic Classes</h4><pre><code>class Box&lt;T&gt; {\n  constructor(public value: T) {}\n}\n\nconst numBox = new Box(100);\nconst strBox = new Box('Angular');</code></pre><h4>Generic Constraints</h4><p>Sometimes we need to ensure the generic type contains certain properties. This is done using <code class=\"inline\">extends</code>.</p><pre><code>function printId&lt;T extends { id: number }&gt;(obj: T) {\n  console.log(obj.id);\n}</code></pre><p>Now only objects containing an <code class=\"inline\">id</code> property are allowed.</p><pre><code>printId({ id: 1, name: 'John' }); // ✅\nprintId({ name: 'John' });         // ❌ Error</code></pre><h4>Multiple Generic Types</h4><pre><code>function pair&lt;K, V&gt;(key: K, value: V) {\n  return { key, value };\n}\n\nconst p = pair('id', 100);</code></pre><h4>Real-world Angular Example</h4><p>Angular's <code class=\"inline\">HttpClient</code> heavily relies on generics.</p><pre><code>interface User {\n  id: number;\n  name: string;\n}\n\nthis.http.get&lt;User[]&gt;('/api/users');</code></pre><p>Because of generics, TypeScript knows the response is an array of <code class=\"inline\">User</code> objects, enabling IntelliSense and compile-time type checking.</p><pre><code>users[0].name; // ✅\nusers[0].salary; // ❌ Property does not exist</code></pre><h4>Why not use any?</h4><table><tr><th>any</th><th>Generics</th></tr><tr><td>Loses type safety</td><td>Maintains type safety</td></tr><tr><td>No IntelliSense</td><td>Full IntelliSense</td></tr><tr><td>Errors found at runtime</td><td>Errors found during compilation</td></tr><tr><td>Not reusable safely</td><td>Reusable and type-safe</td></tr></table><h4>Common Built-in Generic Types</h4><ul><li><code class=\"inline\">Promise&lt;T&gt;</code></li><li><code class=\"inline\">Array&lt;T&gt;</code></li><li><code class=\"inline\">Map&lt;K, V&gt;</code></li><li><code class=\"inline\">Set&lt;T&gt;</code></li><li><code class=\"inline\">Observable&lt;T&gt;</code> (Angular/RxJS)</li></ul><h4>Interview Tip</h4><p>If asked <strong>'Why use Generics instead of any?'</strong>, explain that generics preserve the actual type throughout the program, whereas <code class=\"inline\">any</code> disables type checking completely. Mention Angular's <code class=\"inline\">HttpClient.get&lt;T&gt;()</code> as a real-world example—it's one of the most common follow-up questions.</p><h4>Interview Answer</h4><p><strong>\"Generics allow us to write reusable, type-safe code by using type parameters instead of fixed types or any. The actual type is supplied when the function or class is used, allowing TypeScript to preserve IntelliSense and compile-time checking. In Angular, generics are commonly used with HttpClient, Observables, Promises, and reusable interfaces like API responses.\"</strong></p>"
  },
  {
    "id": "ts-utility-types",
    "category": "typescript",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "utility-types",
      "mapped-types"
    ],
    "question": "Name some built-in utility types.",
    "answer": "<ul><li><code class=\"inline\">Partial&lt;T&gt;</code> — all props optional (patch/update DTOs).</li><li><code class=\"inline\">Required&lt;T&gt;</code> — all props required.</li><li><code class=\"inline\">Pick&lt;T,K&gt;</code> / <code class=\"inline\">Omit&lt;T,K&gt;</code> — select or exclude keys.</li><li><code class=\"inline\">Record&lt;K,V&gt;</code> — a map/dictionary type.</li><li><code class=\"inline\">Readonly&lt;T&gt;</code> — immutable props.</li></ul>",
    "tip": "Partial<T> is perfect for update payloads where only some fields change.",
    "code": "type UserPatch = Partial<User>;\ntype Roles = Record<string, boolean>;",
    "lang": "ts",
    "deep": "<h4>The ones you'll actually be asked about</h4><pre><code>interface User { id: string; name: string; email: string; age: number }\n\nPartial&lt;User&gt;               // every prop optional  -- PATCH payloads, form drafts\nRequired&lt;User&gt;              // every prop required\nReadonly&lt;User&gt;              // every prop readonly  -- immutable state\nPick&lt;User, 'id' | 'name'&gt;   // { id, name }         -- narrow a DTO\nOmit&lt;User, 'id'&gt;            // everything but id    -- \"create\" payloads\nRecord&lt;string, User&gt;        // dictionary           -- NgRx entities\nExclude&lt;'a'|'b'|'c', 'a'&gt;   // 'b' | 'c'            -- unions\nExtract&lt;'a'|'b', 'a'|'z'&gt;   // 'a'\nNonNullable&lt;string|null&gt;    // string\nReturnType&lt;typeof fn&gt;       // fn's return type\nParameters&lt;typeof fn&gt;       // [args] tuple\nAwaited&lt;Promise&lt;User&gt;&gt;      // User</code></pre><h4>They're not magic — you can write them</h4><pre><code>type MyPartial&lt;T&gt;  = { [K in keyof T]?: T[K] };\ntype MyReadonly&lt;T&gt; = { readonly [K in keyof T]: T[K] };\ntype MyPick&lt;T, K extends keyof T&gt; = { [P in K]: T[P] };\ntype MyExclude&lt;T, U&gt; = T extends U ? never : T;   // conditional + distributive</code></pre><p>Being able to reimplement <code class=\"inline\">Partial</code> and <code class=\"inline\">Exclude</code> is the follow-up question — it proves you understand mapped and conditional types rather than memorising a list.</p><h4>The Omit vs Pick gotcha</h4><pre><code>Pick&lt;User, 'nam'&gt;   // ✘ error — 'nam' is not a key of User\nOmit&lt;User, 'nam'&gt;   // ✔ compiles silently! Omit's key param is `keyof any`</code></pre><p><code class=\"inline\">Omit</code> doesn't check that the key exists, so a typo (or a renamed field) silently omits nothing and you never hear about it. <code class=\"inline\">Pick</code> is constrained to <code class=\"inline\">keyof T</code> and does check. That asymmetry is a genuinely good thing to know.</p><h4>Real Angular/NgRx uses</h4><pre><code>// create payload — server assigns the id\ntype CreateUser = Omit&lt;User, 'id' | 'createdAt'&gt;;\n\n// PATCH — any subset, but never the id\ntype UpdateUser = Partial&lt;Omit&lt;User, 'id'&gt;&gt; &amp; { id: string };\n\n// NgRx entity state\ninterface State { entities: Record&lt;string, User&gt;; ids: string[] }\n\n// derive form controls from the model — stays in sync automatically\ntype UserForm = { [K in keyof User]: FormControl&lt;User[K]&gt; };</code></pre><h4>They're shallow</h4><pre><code>Partial&lt;{ a: { b: string } }&gt;   // a?: { b: string } — b is still REQUIRED</code></pre><p><code class=\"inline\">Partial</code>/<code class=\"inline\">Readonly</code> only touch the top level. Deep versions need recursion:</p><pre><code>type DeepPartial&lt;T&gt; = { [K in keyof T]?: T[K] extends object ? DeepPartial&lt;T[K]&gt; : T[K] };</code></pre><h4>How to say it in an interview</h4><p>“The ones I use constantly are <code class=\"inline\">Partial</code> for PATCH payloads, <code class=\"inline\">Omit</code> for create payloads where the server assigns the id, <code class=\"inline\">Pick</code> to narrow a DTO, and <code class=\"inline\">Record</code> for entity dictionaries. They're just mapped and conditional types — <code class=\"inline\">Partial</code> is a one-line mapped type. The trap worth knowing is that <code class=\"inline\">Omit</code> doesn't validate its keys, so a typo silently omits nothing, whereas <code class=\"inline\">Pick</code> is constrained to <code class=\"inline\">keyof T</code> and errors. And they're all shallow.”</p>"
  },
  {
    "id": "ts-enums",
    "category": "typescript",
    "difficulty": "beginner",
    "tags": [
      "V.Imp",
      "enum",
      "union",
      "const-enum",
      "tradeoffs"
    ],
    "question": "What are enums, and what's a union alternative?",
    "answer": "<p><strong>enum</strong> defines a set of named constants. Numeric enums generate reverse mappings; string enums are more readable in logs.</p><p>Many teams prefer a <strong>string literal union</strong> (<code class=\"inline\">type Size = 'sm' | 'md' | 'lg'</code>) — it's lighter and tree-shakable, with no runtime object.</p>",
    "tip": "const enums and string-literal unions avoid emitting runtime code.",
    "code": "enum Role { Admin = 'ADMIN', User = 'USER' }",
    "lang": "ts",
    "deep": "<h4>Why enums are contentious</h4><p>An <code class=\"inline\">enum</code> is one of the very few TypeScript features that <strong>emits JavaScript</strong>. Almost everything else is erased at compile time.</p><pre><code>enum Status { Active, Inactive }\n\n// compiles to a real runtime object:\nvar Status;\n(function (Status) {\n  Status[Status[\"Active\"] = 0] = \"Active\";\n  Status[Status[\"Inactive\"] = 1] = \"Inactive\";\n})(Status || (Status = {}));</code></pre><p>That object is <strong>reverse-mapped</strong> (<code class=\"inline\">Status[0] === 'Active'</code> and <code class=\"inline\">Status.Active === 0</code>), it ships in your bundle, and it can't be tree-shaken. Hence the union alternative:</p><pre><code>const STATUS = ['active', 'inactive'] as const;\ntype Status = typeof STATUS[number];   // 'active' | 'inactive'\n// or simply:\ntype Status = 'active' | 'inactive';   // ZERO runtime output</code></pre><h4>The numeric enum hole</h4><pre><code>enum Status { Active, Inactive }\nconst s: Status = 5;      // ✔ COMPILES. 5 isn't a member!</code></pre><p>Numeric enums accept <strong>any number</strong> — the type safety you came for isn't there. (Fixed for string enums.) A union of string literals rejects anything not listed. This alone is a strong argument.</p><h4>Comparison</h4><table><thead><tr><th></th><th>enum</th><th>string union</th></tr></thead><tbody><tr><td>Runtime code</td><td><strong>yes</strong></td><td><strong>none</strong></td></tr><tr><td>Tree-shakable</td><td>no</td><td>n/a</td></tr><tr><td>Iterate the members</td><td><code class=\"inline\">Object.values(Status)</code></td><td>needs an <code class=\"inline\">as const</code> array</td></tr><tr><td>Accepts invalid values</td><td>numeric: <strong>yes</strong></td><td>no</td></tr><tr><td>Debugging / JSON</td><td>shows <code class=\"inline\">0</code></td><td>shows <code class=\"inline\">'active'</code></td></tr><tr><td><code class=\"inline\">isolatedModules</code> / Babel</td><td><code class=\"inline\">const enum</code> breaks</td><td>fine</td></tr></tbody></table><h4>const enum — and why it's a trap</h4><pre><code>const enum Direction { Up = 'UP' }\nconst d = Direction.Up;   // inlined at compile time -> const d = 'UP'; no runtime object</code></pre><p>Zero runtime cost — but it <strong>breaks under <code class=\"inline\">isolatedModules</code></strong> (Babel, esbuild, SWC, Vite), because inlining needs whole-program knowledge that single-file transpilers don't have. Since Angular's modern builder uses esbuild, this matters. TS 5.0's <code class=\"inline\">enum</code> improvements plus <code class=\"inline\">preserveConstEnums</code> soften it, but the guidance stands: avoid <code class=\"inline\">const enum</code> in libraries.</p><h4>When an enum still wins</h4><ul><li>You need to <strong>iterate</strong> members at runtime (<code class=\"inline\">Object.values</code>) without maintaining a separate array.</li><li>You want a <strong>namespace</strong>: <code class=\"inline\">Status.Active</code> reads better than a bare <code class=\"inline\">'active'</code> scattered around.</li><li>The team already uses them consistently — churn isn't free.</li></ul><h4>The honest position</h4><p>If you use them: <strong>string enums, never numeric</strong>. Otherwise a union of string literals, plus an <code class=\"inline\">as const</code> array when you need to iterate. That combination gives you the namespace, iteration, and zero runtime cost.</p><h4>How to say it in an interview</h4><p>“Enums are one of the few TS features that emit runtime JavaScript — a reverse-mapped object that ships in the bundle and can't be tree-shaken. And numeric enums accept any number, so <code class=\"inline\">const s: Status = 5</code> compiles, which defeats the point. I default to a union of string literals — zero runtime, exact checking, readable in JSON — with an <code class=\"inline\">as const</code> array plus <code class=\"inline\">typeof arr[number]</code> when I need to iterate. If enums are already the house style, string enums only, and I'd avoid <code class=\"inline\">const enum</code> because it breaks under isolatedModules.”</p>"
  },
  {
    "id": "ts-access-modifiers",
    "category": "typescript",
    "difficulty": "beginner",
    "tags": [
      "oop",
      "modifiers"
    ],
    "question": "Explain public, private, protected, readonly.",
    "answer": "<ul><li><strong>public</strong> (default) — accessible anywhere.</li><li><strong>private</strong> — only within the class.</li><li><strong>protected</strong> — within the class and subclasses.</li><li><strong>readonly</strong> — assignable once (in declaration or constructor).</li></ul>",
    "tip": "Parameter properties let you declare + assign in the constructor in one line.",
    "code": "constructor(private http: HttpClient, readonly id: string) {}",
    "lang": "ts"
  },
  {
    "id": "ts-type-narrowing",
    "category": "typescript",
    "difficulty": "intermediate",
    "tags": [
      "V.Imp",
      "narrowing",
      "type-guards",
      "control-flow"
    ],
    "question": "What is type narrowing / type guards?",
    "answer": "<p>Refining a broad type to a specific one within a block using checks like <code class=\"inline\">typeof</code>, <code class=\"inline\">instanceof</code>, <code class=\"inline\">in</code>, or a custom <strong>type predicate</strong> (<code class=\"inline\">x is Cat</code>). TypeScript then knows the precise type inside that branch.</p>",
    "tip": "Custom guards return 'param is Type' to teach the compiler.",
    "code": "function isCat(a: Animal): a is Cat { return 'meow' in a; }",
    "lang": "ts",
    "deep": "<h4>Narrowing = control-flow analysis</h4><p>TypeScript tracks what a type <em>must</em> be along each code path. You don't cast — you prove it, and the compiler follows along.</p><pre><code>function f(x: string | number | null) {\n  if (x === null) return;      // x: string | number\n  if (typeof x === 'string') {\n    x.toUpperCase();           // x: string\n  } else {\n    x.toFixed(2);              // x: number  -- narrowed by elimination\n  }\n}</code></pre><h4>Every narrowing tool</h4><table><thead><tr><th>Tool</th><th>Narrows</th></tr></thead><tbody><tr><td><code class=\"inline\">typeof x === 'string'</code></td><td>primitives</td></tr><tr><td><code class=\"inline\">x instanceof Error</code></td><td>classes</td></tr><tr><td><code class=\"inline\">'kind' in x</code></td><td>by property presence</td></tr><tr><td><code class=\"inline\">x.kind === 'circle'</code></td><td>discriminated unions ← best</td></tr><tr><td><code class=\"inline\">if (x)</code></td><td>truthiness (careful — see below)</td></tr><tr><td><code class=\"inline\">Array.isArray(x)</code></td><td>arrays</td></tr><tr><td><code class=\"inline\">x is Foo</code></td><td>custom type predicate</td></tr><tr><td><code class=\"inline\">asserts x is Foo</code></td><td>assertion function</td></tr></tbody></table><h4>Custom type guards</h4><pre><code>// The `x is User` return type is what teaches the compiler\nfunction isUser(x: unknown): x is User {\n  return typeof x === 'object' &amp;&amp; x !== null &amp;&amp; 'id' in x;\n}\n\nif (isUser(data)) data.id;   // ✔ data: User</code></pre><p><strong>The catch:</strong> a type predicate is an <em>unchecked promise</em>. Return <code class=\"inline\">true</code> wrongly and TypeScript believes you — it's an assertion with nicer syntax, not a runtime guarantee. For real external data, validate with <strong>Zod</strong> and infer the type from the schema; that's the answer for \"how do you type an API response safely?\"</p><h4>The truthiness trap</h4><pre><code>function f(x: string | number) {\n  if (x) { /* x is string | number — 0 and '' fall to the else! */ }\n}\n// Correct:\nif (x !== undefined) { }</code></pre><p><code class=\"inline\">if (count)</code> is a bug when <code class=\"inline\">0</code> is valid. Same for <code class=\"inline\">''</code>. Narrow explicitly against <code class=\"inline\">null</code>/<code class=\"inline\">undefined</code>.</p><h4>Narrowing gets lost — this surprises people</h4><pre><code>if (this.user !== null) {\n  doSomething();\n  this.user.name;      // ✘ Object is possibly 'null'\n}</code></pre><p>Narrowing on a <strong>class property</strong> is discarded after any function call, because TS can't prove the callback didn't reassign it. Fix: copy to a <code class=\"inline\">const</code> first — <code class=\"inline\">const u = this.user; if (u) u.name;</code>. A <code class=\"inline\">const</code> can't be reassigned, so the narrowing sticks. This is a very common Angular annoyance.</p><h4>How to say it in an interview</h4><p>“Narrowing is control-flow analysis — <code class=\"inline\">typeof</code>, <code class=\"inline\">instanceof</code>, <code class=\"inline\">in</code>, or a discriminant check, and the compiler tracks the type down each branch so I never need a cast. For custom checks I write a predicate returning <code class=\"inline\">x is User</code>, but that's an unchecked promise — for real API data I'd validate with Zod and infer the type. Two traps: truthiness checks swallow <code class=\"inline\">0</code> and <code class=\"inline\">''</code>, and narrowing on a class property is lost after any function call, so I copy it into a <code class=\"inline\">const</code> first.”</p>"
  },
  {
    "id": "ts-unknown-vs-any-return",
    "category": "typescript",
    "difficulty": "intermediate",
    "tags": [
      "union",
      "intersection"
    ],
    "question": "Union vs intersection types.",
    "answer": "<p><strong>Union</strong> (<code class=\"inline\">A | B</code>) — the value is one of the types. <strong>Intersection</strong> (<code class=\"inline\">A &amp; B</code>) — the value has all properties of both.</p>",
    "tip": "Union = OR, intersection = AND.",
    "code": "type Id = string | number;      // union\ntype Employee = Person & Staff;  // intersection",
    "lang": "ts"
  },
  {
    "id": "ts-optional-vs-undefined",
    "category": "typescript",
    "difficulty": "beginner",
    "tags": [
      "optional",
      "strict"
    ],
    "question": "What does strictNullChecks do?",
    "answer": "<p>With <code class=\"inline\">strictNullChecks</code> on, <code class=\"inline\">null</code> and <code class=\"inline\">undefined</code> are not assignable to other types unless explicitly included — the compiler forces you to handle empty values, preventing a whole class of runtime errors.</p>",
    "tip": "It's part of the recommended strict: true preset — always enable it.",
    "code": "let name: string | null = null; // only allowed because of the union",
    "lang": "ts"
  },
  {
    "id": "ts-mapped-types",
    "category": "typescript",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "mapped-types",
      "generics",
      "advanced"
    ],
    "question": "What are mapped types?",
    "answer": "<p>Types that transform each property of another type. Utility types like <code class=\"inline\">Partial</code> and <code class=\"inline\">Readonly</code> are mapped types under the hood.</p>",
    "tip": "Combine with keyof to iterate a type's keys.",
    "code": "type Optional<T> = { [K in keyof T]?: T[K] };",
    "lang": "ts",
    "deep": "<h4>The shape</h4><pre><code>type MyReadonly&lt;T&gt; = { readonly [K in keyof T]: T[K] };\n//                     ^modifier ^^^^^^^^^^^^^ iterate keys  ^lookup type</code></pre><p><code class=\"inline\">[K in keyof T]</code> is a <code class=\"inline\">for…in</code> at the type level: for each key <code class=\"inline\">K</code> of <code class=\"inline\">T</code>, produce a property whose type is <code class=\"inline\">T[K]</code> (an <em>indexed access</em>). That's the entire mechanism — <code class=\"inline\">Partial</code>, <code class=\"inline\">Readonly</code>, <code class=\"inline\">Pick</code> and <code class=\"inline\">Record</code> are all one-liners built on it.</p><h4>Modifiers — add and remove</h4><pre><code>type Partial&lt;T&gt;  = { [K in keyof T]?: T[K] };        // add ?\ntype Required&lt;T&gt; = { [K in keyof T]-?: T[K] };       // REMOVE ?\ntype Mutable&lt;T&gt;  = { -readonly [K in keyof T]: T[K] }; // REMOVE readonly</code></pre><p>The <code class=\"inline\">-?</code> and <code class=\"inline\">-readonly</code> syntax is the part people forget. <code class=\"inline\">+</code> is the (implicit) default.</p><h4>Key remapping with <code class=\"inline\">as</code> (TS 4.1+)</h4><pre><code>type Getters&lt;T&gt; = {\n  [K in keyof T as `get${Capitalize&lt;string &amp; K&gt;}`]: () =&gt; T[K]\n};\n\ntype G = Getters&lt;{ name: string; age: number }&gt;;\n// { getName: () =&gt; string; getAge: () =&gt; number }</code></pre><p>Combined with template literal types, this generates key names. Filter keys by mapping to <code class=\"inline\">never</code>:</p><pre><code>// keep only the string-valued properties\ntype StringProps&lt;T&gt; = {\n  [K in keyof T as T[K] extends string ? K : never]: T[K]\n};</code></pre><h4>The Angular use that makes it click</h4><pre><code>// Typed reactive form derived from the model — can never drift out of sync\ntype ControlsOf&lt;T&gt; = { [K in keyof T]: FormControl&lt;T[K]&gt; };\n\ninterface User { name: string; age: number }\nconst form = new FormGroup&lt;ControlsOf&lt;User&gt;&gt;({\n  name: new FormControl('', { nonNullable: true }),\n  age:  new FormControl(0,  { nonNullable: true })\n});\n// Add a field to User -&gt; the FormGroup fails to compile until you add the control.</code></pre><p>That's the payoff: one source of truth, and the compiler enforces the rest.</p><h4>Distribution over unions</h4><pre><code>type Nullable&lt;T&gt; = { [K in keyof T]: T[K] | null };\n// Homomorphic mapped types preserve optionality/readonly from T — and map\n// over each member when T is a union.</code></pre><h4>How to say it in an interview</h4><p>“It's a <code class=\"inline\">for…in</code> for types — <code class=\"inline\">[K in keyof T]</code> iterates the keys and <code class=\"inline\">T[K]</code> looks up each value type. That's all <code class=\"inline\">Partial</code> and <code class=\"inline\">Readonly</code> are; you can add or strip modifiers with <code class=\"inline\">?</code>, <code class=\"inline\">-?</code> and <code class=\"inline\">-readonly</code>. Since 4.1 you can remap keys with <code class=\"inline\">as</code> plus template literal types to generate names, or map to <code class=\"inline\">never</code> to filter. Where I actually use it is deriving a typed FormGroup from a model interface, so adding a field to the model breaks the form until I add the control.”</p>"
  },
  {
    "id": "ts-keyof-typeof",
    "category": "typescript",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "keyof",
      "typeof",
      "types"
    ],
    "question": "What do keyof and typeof do at the type level?",
    "answer": "<p><code class=\"inline\">keyof T</code> produces a union of a type's property names. Type-level <code class=\"inline\">typeof value</code> captures the type of an existing value/const so you can derive types from data.</p>",
    "tip": "typeof + keyof lets you build types from a constant config object.",
    "code": "const config = { host: '', port: 0 };\ntype ConfigKey = keyof typeof config; // 'host' | 'port'",
    "lang": "ts",
    "deep": "<h4>Two operators, two worlds</h4><ul><li><code class=\"inline\">typeof</code> — lifts a <strong>value</strong> into the type world.</li><li><code class=\"inline\">keyof</code> — takes a <strong>type</strong> and gives the union of its keys.</li></ul><pre><code>const config = { host: 'localhost', port: 8080 };\n\ntype Config = typeof config;        // { host: string; port: number }\ntype Key    = keyof Config;         // 'host' | 'port'\ntype Key2   = keyof typeof config;  // 'host' | 'port'  -- the common combo</code></pre><p>Note this is <strong>not</strong> JavaScript's runtime <code class=\"inline\">typeof</code>. Same keyword, different world — TypeScript decides which by <em>position</em>: in a type annotation it's the type operator, in an expression it's the JS one.</p><h4>Why <code class=\"inline\">keyof typeof</code> is the everyday combo</h4><p><code class=\"inline\">keyof</code> needs a <em>type</em>, but you usually have a <em>value</em> (a const object). So you lift it first: <code class=\"inline\">keyof typeof config</code>. That's the idiom you'll write constantly.</p><h4>The payoff: derive types, don't duplicate them</h4><pre><code>const ROLES = { admin: 'Admin', user: 'User', guest: 'Guest' } as const;\n\ntype Role = keyof typeof ROLES;          // 'admin' | 'user' | 'guest'\ntype Label = typeof ROLES[Role];         // 'Admin' | 'User' | 'Guest'\n\nfunction label(r: Role) { return ROLES[r]; }\nlabel('admin');   // ✔\nlabel('root');    // ✘ compile error</code></pre><p>Add a role to the object and the type updates itself — <strong>one source of truth</strong>. The alternative is a hand-maintained union that silently drifts. That's the whole argument.</p><h4>Typed property access — the classic generic</h4><pre><code>function get&lt;T, K extends keyof T&gt;(obj: T, key: K): T[K] {\n  return obj[key];\n}\n\nconst user = { id: 1, name: 'Ann' };\nget(user, 'name');   // string — return type follows the key!\nget(user, 'nope');   // ✘ Argument of type '\"nope\"' is not assignable</code></pre><p><code class=\"inline\">T[K]</code> is an <strong>indexed access type</strong> — the return type is computed from which key you passed. This is the standard \"show me a useful generic\" answer.</p><h4>Gotchas</h4><pre><code>keyof any                        // string | number | symbol\nkeyof { [k: string]: number }    // string | number  (not just string!)\nkeyof string[]                   // number | 'length' | 'push' | ... — all Array members</code></pre><p>The <code class=\"inline\">as const</code> part is essential: without it, <code class=\"inline\">typeof ROLES</code> widens values to <code class=\"inline\">string</code> and <code class=\"inline\">typeof ROLES[Role]</code> becomes <code class=\"inline\">string</code>, losing the literals.</p><h4>How to say it in an interview</h4><p>“<code class=\"inline\">typeof</code> lifts a value into the type world; <code class=\"inline\">keyof</code> gives the union of a type's keys — so <code class=\"inline\">keyof typeof myObject</code> is the everyday combination. I use it to derive types from a const object instead of hand-writing a union that drifts: define <code class=\"inline\">ROLES</code> with <code class=\"inline\">as const</code>, and <code class=\"inline\">keyof typeof ROLES</code> stays correct forever. Paired with <code class=\"inline\">T[K]</code> indexed access it gives you a typed <code class=\"inline\">get(obj, key)</code> whose return type follows the key.”</p>"
  },
  {
    "id": "ts-decorators",
    "category": "typescript",
    "difficulty": "intermediate",
    "tags": [
      "decorators",
      "angular"
    ],
    "question": "What are decorators (as used in Angular)?",
    "answer": "<p>Decorators are functions that add metadata/behaviour to classes, methods, properties, or parameters. Angular relies on them heavily: <code class=\"inline\">@Component</code>, <code class=\"inline\">@Injectable</code>, <code class=\"inline\">@Input</code>, <code class=\"inline\">@Output</code>.</p>",
    "tip": "They require experimentalDecorators / emitDecoratorMetadata in tsconfig (Angular sets this up).",
    "code": "@Input() label = '';\n@Output() change = new EventEmitter();",
    "lang": "ts"
  },
  {
    "id": "ts-as-const",
    "category": "typescript",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "as-const",
      "literal-types",
      "inference"
    ],
    "question": "What does 'as const' do?",
    "answer": "<p>A <strong>const assertion</strong> makes a value deeply readonly and narrows it to its literal types instead of the widened primitive — useful for building precise union types from arrays/objects.</p>",
    "tip": "Great for turning a list of strings into a literal union type.",
    "code": "const sizes = ['sm', 'md', 'lg'] as const;\ntype Size = typeof sizes[number]; // 'sm' | 'md' | 'lg'",
    "lang": "ts",
    "deep": "<h4>The problem: widening</h4><pre><code>let   a = 'hello';         // string          -- widened\nconst b = 'hello';         // 'hello'         -- literal, because const can't change\n\nconst obj = { status: 'ok' };\nobj.status                 // string!  -- the PROPERTY is mutable, so it widens\n\nconst arr = [1, 2];        // number[] -- not [1, 2]</code></pre><p>TypeScript widens literals to their base type whenever the value could later change. A <code class=\"inline\">const</code> <em>binding</em> can't be reassigned, so it keeps the literal — but a <code class=\"inline\">const</code> object's <em>properties</em> can, so they widen.</p><h4>What as const does</h4><pre><code>const obj = { status: 'ok' } as const;\n// { readonly status: 'ok' }\n\nconst arr = [1, 2] as const;\n// readonly [1, 2]   -- a readonly TUPLE, not number[]</code></pre><p>It does three things at once: <strong>literal types</strong> instead of widened ones, <strong>readonly</strong> on every property, and <strong>tuples</strong> instead of arrays — recursively, all the way down.</p><h4>Where it actually matters</h4><pre><code>// 1. Deriving a union from a const object (the big one)\nconst ROLES = { admin: 'Admin', user: 'User' } as const;\ntype Role = keyof typeof ROLES;        // 'admin' | 'user'\ntype Label = typeof ROLES[Role];       // 'Admin' | 'User'\n// without as const -&gt; Label is just `string`\n\n// 2. Passing a literal where a literal is required\nconst config = { method: 'GET' };\nfetch(url, config);        // ✘ string not assignable to RequestMethod\nconst config2 = { method: 'GET' } as const;   // ✔\n\n// 3. Discriminated unions\nconst action = { type: 'increment' } as const;   // type: 'increment', not string\n// A `type: string` discriminant narrows NOTHING.\n\n// 4. Tuple returns (custom hooks / helpers)\nfunction useToggle() {\n  return [value, toggle] as const;   // [boolean, () =&gt; void]\n}                                    // without it: (boolean | (() =&gt; void))[]</code></pre><p>#4 is why every React-style tuple hook ends in <code class=\"inline\">as const</code> — otherwise destructuring gives you a useless union type.</p><h4>as const vs readonly vs Object.freeze</h4><table><thead><tr><th></th><th>Literal types</th><th>Compile-time immutable</th><th>Runtime immutable</th></tr></thead><tbody><tr><td><code class=\"inline\">as const</code></td><td>✔</td><td>✔</td><td><strong>✘</strong></td></tr><tr><td><code class=\"inline\">readonly</code> modifier</td><td>✘</td><td>✔</td><td>✘</td></tr><tr><td><code class=\"inline\">Object.freeze()</code></td><td>partly</td><td>✔</td><td><strong>✔</strong></td></tr></tbody></table><p><code class=\"inline\">as const</code> is <strong>erased at compile time</strong> — it's zero runtime protection. Cast it away and you can mutate freely. It's a hint to the inference engine, not a guarantee.</p><h4>It's not a type assertion</h4><p>Despite the <code class=\"inline\">as</code> keyword, it isn't an unsafe cast like <code class=\"inline\">as User</code> — it can't lie about the type. It only tells TypeScript \"infer this as narrowly as possible\".</p><h4>How to say it in an interview</h4><p>“It stops literal widening — you get <code class=\"inline\">'ok'</code> instead of <code class=\"inline\">string</code>, readonly properties, and tuples instead of arrays, recursively. I use it most for deriving a union from a const object with <code class=\"inline\">keyof typeof</code>, which keeps one source of truth, and on tuple returns so destructuring gets the right types. Worth saying it's compile-time only — it's not <code class=\"inline\">Object.freeze</code>, there's no runtime protection.”</p>"
  },
  {
    "id": "ts-non-null",
    "category": "typescript",
    "difficulty": "beginner",
    "tags": [
      "V.Imp",
      "non-null-assertion",
      "strictNullChecks",
      "safety"
    ],
    "question": "What is the non-null assertion operator (!)?",
    "answer": "<p>The postfix <code class=\"inline\">!</code> tells the compiler 'this value is definitely not null/undefined here'. It removes the check but does <em>not</em> add a runtime guard — use it only when you're certain.</p>",
    "tip": "Overusing ! defeats strictNullChecks — narrow properly when you can.",
    "code": "@ViewChild('el') el!: ElementRef; // asserted, assigned by Angular later",
    "lang": "ts",
    "deep": "<h4>What it does — and doesn't</h4><pre><code>const el = document.getElementById('app')!;   // HTMLElement (not | null)\nel.innerHTML = 'hi';</code></pre><p><code class=\"inline\">!</code> tells the compiler \"trust me, this isn't null\". It is <strong>erased at compile time</strong> and generates <strong>no runtime check whatsoever</strong>. If you're wrong, you get the exact <code class=\"inline\">TypeError: Cannot read properties of null</code> that <code class=\"inline\">strictNullChecks</code> was there to prevent.</p><p>It doesn't make anything safe. It <em>silences the compiler</em>.</p><h4>Don't confuse the three <code class=\"inline\">!</code>/<code class=\"inline\">?</code> operators</h4><table><thead><tr><th>Syntax</th><th>Name</th><th>Runtime effect</th></tr></thead><tbody><tr><td><code class=\"inline\">x!</code></td><td>non-null assertion</td><td><strong>none</strong> — erased</td></tr><tr><td><code class=\"inline\">x?.y</code></td><td>optional chaining</td><td><strong>real check</strong> → <code class=\"inline\">undefined</code></td></tr><tr><td><code class=\"inline\">x ?? y</code></td><td>nullish coalescing</td><td><strong>real check</strong> → fallback</td></tr><tr><td><code class=\"inline\">x!: T</code></td><td>definite assignment</td><td>none — a promise to the compiler</td></tr></tbody></table><p><code class=\"inline\">?.</code> and <code class=\"inline\">??</code> emit real JavaScript. <code class=\"inline\">!</code> emits nothing. That's the distinction interviewers probe.</p><h4>The honest uses</h4><pre><code>// 1. You genuinely know better than the compiler\nconst el = document.getElementById('root')!;   // it's in index.html\n\n// 2. Definite assignment — Angular @ViewChild / @Input\n@ViewChild('chart') chart!: ElementRef;   // Angular assigns it after construction\n@Input() user!: User;                     // the parent must provide it\n\n// 3. After a check the compiler can't follow\nif (map.has(k)) { map.get(k)!.doThing(); }   // TS can't link has() to get()</code></pre><p>Case 2 is why you see <code class=\"inline\">!</code> everywhere in Angular: <code class=\"inline\">strictPropertyInitialization</code> demands assignment in the constructor, but the framework assigns these later. It's the idiomatic use — though <code class=\"inline\">@Input({ required: true })</code> (Angular 16+) is now better for inputs.</p><h4>Why reviewers flag it</h4><pre><code>// The lie that ages badly\nconst user = users.find(u =&gt; u.id === id)!;   // \"there's always a match\"\nuser.name;                                     // ...until there isn't. Runtime crash.</code></pre><p><code class=\"inline\">!</code> is a <strong>claim frozen in time</strong>. It may be true today and false after a refactor — and nothing re-checks it. Prefer code that makes the compiler agree:</p><pre><code>const user = users.find(u =&gt; u.id === id);\nif (!user) throw new Error(`User ${id} not found`);   // narrowed AND safe at runtime\nuser.name;   // ✔ no assertion needed</code></pre><p>That version fails <em>loudly and specifically</em> instead of crashing three frames later with a useless message.</p><h4>The double-bang confusion</h4><pre><code>!!value    // JS: coerce to boolean — completely unrelated\nvalue!     // TS: non-null assertion</code></pre><h4>How to say it in an interview</h4><p>“It asserts a value isn't null, and it's erased — no runtime check at all, unlike <code class=\"inline\">?.</code> and <code class=\"inline\">??</code>, which emit real JavaScript. So it doesn't make code safe, it silences the compiler. The legitimate use is definite assignment, like Angular's <code class=\"inline\">@ViewChild</code> where the framework assigns after construction. Where I avoid it is things like <code class=\"inline\">find(...)!</code> — that's a claim that's true until someone refactors. I'd rather narrow with an explicit throw, so it fails loudly with a useful message.”</p>"
  },
  {
    "id": "ts-discriminated-union",
    "category": "typescript",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "union",
      "discriminated",
      "narrowing",
      "real-world"
    ],
    "question": "What is a discriminated (tagged) union?",
    "answer": "<p>A union of object types that share a common literal <em>tag</em> field. Switching on that tag lets TypeScript narrow to the exact member, giving type-safe, exhaustive handling — great for state machines and Redux/NgRx actions.</p>",
    "tip": "Add a `never` default case so the compiler errors if you forget a variant.",
    "code": "type Shape =\n  | { kind: 'circle'; r: number }\n  | { kind: 'square'; side: number };\nfunction area(s: Shape) {\n  switch (s.kind) {\n    case 'circle': return Math.PI * s.r ** 2;\n    case 'square': return s.side ** 2;\n  }\n}",
    "lang": "ts",
    "deep": "<h4>The pattern</h4><pre><code>type State =\n  | { status: 'idle' }\n  | { status: 'loading' }\n  | { status: 'success'; data: User[] }      // data ONLY exists here\n  | { status: 'error';   message: string };  // message ONLY exists here\n\nfunction render(s: State) {\n  switch (s.status) {\n    case 'idle':    return 'Start';\n    case 'loading': return 'Spinner';\n    case 'success': return s.data.length;      // ✔ s.data known to exist\n    case 'error':   return s.message;          // ✔ s.message known\n  }\n}</code></pre><p>The <strong>discriminant</strong> is the shared literal field (<code class=\"inline\">status</code>). Checking it narrows the union to exactly one member, and each member's own fields become available.</p><h4>Why this beats the \"bag of optionals\"</h4><pre><code>// ✘ The shape everyone writes first\ninterface State {\n  loading: boolean;\n  data?: User[];\n  error?: string;\n}</code></pre><p>This type permits <strong>impossible states</strong>: <code class=\"inline\">{ loading: true, data: [...], error: 'x' }</code> type-checks fine. So every consumer must defensively handle combinations that should never exist, and you get bugs like a spinner rendering on top of an error. The discriminated union makes those states <strong>unrepresentable</strong> — the compiler rejects them.</p><p>That phrase — <em>\"make illegal states unrepresentable\"</em> — is the point of the question.</p><h4>Exhaustiveness: the compiler as your checklist</h4><pre><code>function render(s: State): string {\n  switch (s.status) {\n    case 'idle':    return 'Start';\n    case 'loading': return 'Spinner';\n    case 'success': return String(s.data.length);\n    case 'error':   return s.message;\n    default:\n      const _exhaustive: never = s;   // ✘ if a variant is unhandled\n      throw new Error(`Unhandled: ${JSON.stringify(s)}`);\n  }\n}</code></pre><p>Add <code class=\"inline\">{ status: 'refreshing' }</code> to the union and this <strong>fails to compile</strong> — <code class=\"inline\">s</code> is no longer <code class=\"inline\">never</code>. The compiler points at every switch that needs updating. Adding a variant becomes a guided refactor instead of a hunt.</p><p>(Alternatively, a <code class=\"inline\">: string</code> return type plus <code class=\"inline\">noImplicitReturns</code> catches it too.)</p><h4>Where you've already used it</h4><ul><li><strong>NgRx actions</strong> — the union of all actions discriminated by <code class=\"inline\">type</code>. That's exactly why a reducer's <code class=\"inline\">action.payload</code> is correctly typed per case.</li><li><strong>Redux</strong>, and any request state machine.</li><li>API responses: <code class=\"inline\">{ ok: true; data } | { ok: false; error }</code>.</li></ul><h4>The requirement</h4><p>The discriminant must be a <strong>literal type</strong> — <code class=\"inline\">'success'</code>, not <code class=\"inline\">string</code>. A <code class=\"inline\">status: string</code> field narrows nothing, which is why <code class=\"inline\">as const</code> and literal unions matter here.</p><h4>How to say it in an interview</h4><p>“A union of object types sharing a literal tag, so checking the tag narrows to exactly one member and its fields. The value is that it makes illegal states unrepresentable — the usual <code class=\"inline\">{ loading, data?, error? }</code> shape permits loading <em>and</em> error at once, and every consumer has to defend against it. Pair it with a <code class=\"inline\">never</code> assignment in the default case and the compiler tells you every place to update when you add a variant. NgRx's action union is exactly this pattern.”</p>"
  },
  {
    "id": "ts-conditional-types",
    "category": "typescript",
    "difficulty": "advanced",
    "tags": [
      "conditional-types",
      "generics",
      "advanced"
    ],
    "question": "What are conditional types?",
    "answer": "<p>Types that branch based on a condition: <code class=\"inline\">T extends U ? X : Y</code>. They power much of the standard library (e.g. <code class=\"inline\">Exclude</code>, <code class=\"inline\">ReturnType</code>) and let generic types adapt to their inputs.</p>",
    "tip": "Combine with `infer` to extract a type from within another.",
    "code": "type NonNull<T> = T extends null | undefined ? never : T;\ntype A = NonNull<string | null>; // string",
    "lang": "ts",
    "deep": "<h4>A ternary for types</h4><pre><code>type IsString&lt;T&gt; = T extends string ? 'yes' : 'no';\ntype A = IsString&lt;'hi'&gt;;   // 'yes'\ntype B = IsString&lt;42&gt;;     // 'no'</code></pre><p><code class=\"inline\">T extends U</code> here means <em>\"is T assignable to U?\"</em> — not class inheritance.</p><h4>The behaviour that trips everyone: distribution</h4><pre><code>type ToArray&lt;T&gt; = T extends any ? T[] : never;\ntype R = ToArray&lt;string | number&gt;;\n// NOT (string | number)[]\n// IS  string[] | number[]</code></pre><p>When the checked type is a <strong>naked type parameter</strong> and you pass a union, the conditional <strong>distributes over each member</strong> and re-unions the results. This is how <code class=\"inline\">Exclude</code> works:</p><pre><code>type Exclude&lt;T, U&gt; = T extends U ? never : T;\n\nExclude&lt;'a'|'b'|'c', 'a'&gt;\n// distributes: ('a' extends 'a' ? never : 'a')   -&gt; never\n//            | ('b' extends 'a' ? never : 'b')   -&gt; 'b'\n//            | ('c' extends 'a' ? never : 'c')   -&gt; 'c'\n// = 'b' | 'c'      (never vanishes from a union)</code></pre><h4>Turning distribution off</h4><pre><code>type NoDist&lt;T&gt; = [T] extends [any] ? T[] : never;\ntype R2 = NoDist&lt;string | number&gt;;   // (string | number)[]</code></pre><p>Wrapping both sides in a tuple makes the parameter non-naked, so it's checked as a whole. That's the trick behind <code class=\"inline\">IsNever</code> and most \"type-level equality\" helpers.</p><h4>With infer</h4><pre><code>type ElementOf&lt;T&gt; = T extends (infer U)[] ? U : never;\ntype E = ElementOf&lt;string[]&gt;;   // string\n\n// That's how the built-ins are written:\ntype ReturnType&lt;T&gt; = T extends (...a: any) =&gt; infer R ? R : never;\ntype Awaited&lt;T&gt;    = T extends Promise&lt;infer U&gt; ? Awaited&lt;U&gt; : T;  // recursive</code></pre><h4>Where it's actually useful</h4><pre><code>// Unwrap an NgRx action's props\ntype PropsOf&lt;T&gt; = T extends ActionCreator&lt;any, (...a: any) =&gt; infer R&gt; ? R : never;\n\n// Type an API client from a route map\ntype Response&lt;R extends keyof Routes&gt; = Routes[R] extends { res: infer T } ? T : never;</code></pre><h4>The honest framing</h4><p>You <em>write</em> these rarely — mostly in library code or a shared types file. You <em>read</em> them constantly, because that's what the built-in utility types are. Claiming you use conditional types daily in app code is a red flag; saying \"I reach for them when building a typed API layer\" is credible.</p><h4>How to say it in an interview</h4><p>“A ternary at the type level — <code class=\"inline\">T extends U ? X : Y</code>, where extends means assignable. The behaviour that matters is distribution: with a naked type parameter and a union, it applies to each member and re-unions the result, which is exactly how <code class=\"inline\">Exclude</code> works. Wrap it in a tuple to opt out. Combined with <code class=\"inline\">infer</code> you get <code class=\"inline\">ReturnType</code> and <code class=\"inline\">Awaited</code>. I mostly write them in shared type utilities rather than day-to-day app code.”</p>"
  },
  {
    "id": "ts-infer",
    "category": "typescript",
    "difficulty": "advanced",
    "tags": [
      "infer",
      "conditional-types",
      "advanced"
    ],
    "question": "What does the infer keyword do?",
    "answer": "<p>Inside a conditional type, <code class=\"inline\">infer</code> captures a type from a position so you can reuse it — e.g. extract a function's return type or an array's element type.</p>",
    "tip": "infer is how ReturnType<T> and Parameters<T> are implemented.",
    "code": "type Return<T> = T extends (...args: any[]) => infer R ? R : never;\ntype R = Return<() => number>; // number",
    "lang": "ts",
    "deep": "<h4>What it does</h4><p><code class=\"inline\">infer</code> declares a <strong>type variable to be filled in by pattern matching</strong>, inside the <code class=\"inline\">extends</code> clause of a conditional type. It's destructuring, at the type level.</p><pre><code>type ElementOf&lt;T&gt; = T extends (infer U)[] ? U : never;\n//                                  ^^^^^^^ \"whatever the element type is, call it U\"\n\nElementOf&lt;string[]&gt;   // string\nElementOf&lt;number&gt;     // never — didn't match the pattern</code></pre><h4>The built-ins are all just infer</h4><pre><code>type ReturnType&lt;T&gt;  = T extends (...args: any) =&gt; infer R ? R : never;\ntype Parameters&lt;T&gt;  = T extends (...args: infer P) =&gt; any ? P : never;\ntype Awaited&lt;T&gt;     = T extends Promise&lt;infer U&gt; ? Awaited&lt;U&gt; : T;   // recursive!\ntype InstanceType&lt;T&gt; = T extends new (...a: any) =&gt; infer R ? R : never;</code></pre><p>Knowing <code class=\"inline\">ReturnType</code> is one line of <code class=\"inline\">infer</code> is the point of the question — these aren't compiler magic.</p><h4>Multiple and nested infers</h4><pre><code>type First&lt;T&gt; = T extends [infer F, ...any[]] ? F : never;\ntype Last&lt;T&gt;  = T extends [...any[], infer L] ? L : never;\nFirst&lt;[string, number, boolean]&gt;   // string\nLast&lt;[string, number, boolean]&gt;    // boolean\n\n// Deeply unwrap nested promises\ntype DeepAwait&lt;T&gt; = T extends Promise&lt;infer U&gt; ? DeepAwait&lt;U&gt; : T;\nDeepAwait&lt;Promise&lt;Promise&lt;string&gt;&gt;&gt;   // string</code></pre><h4>Template literal inference (TS 4.1+)</h4><pre><code>type RouteParam&lt;T&gt; = T extends `${string}/:${infer P}` ? P : never;\nRouteParam&lt;'/users/:id'&gt;   // 'id'</code></pre><p>This is how typed router libraries extract params from a path string — a genuinely impressive, concrete example.</p><h4>The rule that catches people</h4><p><code class=\"inline\">infer</code> is <strong>only legal inside the <code class=\"inline\">extends</code> clause of a conditional type</strong>. You can't use it in a type alias body or a mapped type directly.</p><pre><code>type Bad&lt;T&gt; = infer U;   // ✘ 'infer' declarations are only permitted\n                         //    in the 'extends' clause of a conditional type</code></pre><h4>Multiple candidates</h4><pre><code>type Both&lt;T&gt; = T extends { a: infer U; b: infer U } ? U : never;\nBoth&lt;{ a: string; b: number }&gt;   // string | number — union for covariant positions</code></pre><h4>How to say it in an interview</h4><p>“It's pattern matching in a conditional type — you write the shape you expect and <code class=\"inline\">infer</code> captures a piece of it. <code class=\"inline\">ReturnType</code> is literally <code class=\"inline\">T extends (...a: any) =&gt; infer R ? R : never</code>. It only works inside the <code class=\"inline\">extends</code> clause of a conditional. It gets powerful with recursion and template literals — you can pull <code class=\"inline\">'id'</code> out of <code class=\"inline\">'/users/:id'</code> at the type level, which is how typed routers work.”</p>"
  },
  {
    "id": "ts-index-signature",
    "category": "typescript",
    "difficulty": "intermediate",
    "tags": [
      "index-signature",
      "records"
    ],
    "question": "What is an index signature?",
    "answer": "<p>Declares that an object can have arbitrary keys of a given type: <code class=\"inline\">{ [key: string]: number }</code>. Useful for dictionaries when keys aren't known ahead of time. <code class=\"inline\">Record&lt;K, V&gt;</code> is the shorthand.</p>",
    "tip": "Prefer Record<string, T> for readability over a raw index signature.",
    "code": "interface Scores { [player: string]: number; }\nconst s: Scores = { alice: 10, bob: 7 };",
    "lang": "ts"
  },
  {
    "id": "ts-tuple",
    "category": "typescript",
    "difficulty": "intermediate",
    "tags": [
      "tuple",
      "types"
    ],
    "question": "What are tuple types?",
    "answer": "<p>Fixed-length arrays with a known type per position — <code class=\"inline\">[string, number]</code>. Support optional and rest elements, and labels for readability. React's <code class=\"inline\">useState</code> return is a tuple.</p>",
    "tip": "Tuples are how you type a function that returns a pair, like [value, setter].",
    "code": "type Result = [ok: boolean, message: string];\nconst r: Result = [true, 'saved'];",
    "lang": "ts"
  },
  {
    "id": "ts-function-overload",
    "category": "typescript",
    "difficulty": "advanced",
    "tags": [
      "overloads",
      "functions"
    ],
    "question": "What are function overloads?",
    "answer": "<p>Multiple type signatures for one implementation, so callers get precise return types based on their arguments. Declare the overload signatures, then one compatible implementation signature.</p>",
    "tip": "Prefer a union or generic if overloads get unwieldy — they're a last resort.",
    "code": "function get(id: number): User;\nfunction get(ids: number[]): User[];\nfunction get(x: number | number[]) { /* impl */ }",
    "lang": "ts"
  },
  {
    "id": "ts-abstract-class",
    "category": "typescript",
    "difficulty": "intermediate",
    "tags": [
      "oop",
      "abstract"
    ],
    "question": "Abstract class vs interface in TypeScript.",
    "answer": "<p>An <strong>abstract class</strong> can't be instantiated and may provide shared implementation plus <code class=\"inline\">abstract</code> members subclasses must implement. An <strong>interface</strong> is purely a compile-time contract with no implementation and vanishes at runtime.</p>",
    "tip": "Use an abstract class to share base logic; an interface just to enforce a shape.",
    "code": "abstract class Base { abstract load(): void; log() {} }",
    "lang": "ts"
  },
  {
    "id": "ts-satisfies",
    "category": "typescript",
    "difficulty": "advanced",
    "tags": [
      "V.Imp",
      "satisfies",
      "inference",
      "modern"
    ],
    "question": "What does the satisfies operator do?",
    "answer": "<p>(TS 4.9+) <code class=\"inline\">satisfies</code> checks that a value matches a type <strong>without widening</strong> it — you keep the precise inferred literal types while still validating the shape. Best of both worlds vs a plain annotation.</p>",
    "tip": "Use satisfies for config objects so keys/values keep their literal types.",
    "code": "const routes = { home: '/', about: '/about' } satisfies Record<string, string>;\n// routes.home is '/', not just string",
    "lang": "ts",
    "deep": "<h4>The problem it solves</h4><p>Before TS 4.9 you had to choose between <strong>validation</strong> and <strong>precise inference</strong>. You couldn't have both.</p><pre><code>type Config = Record&lt;string, string | number&gt;;\n\n// Option A: annotate -&gt; validated, but inference is DESTROYED\nconst a: Config = { host: 'localhost', port: 8080 };\na.host.toUpperCase();   // ✘ string | number — the literal type is gone\na.hosts;                // ✘ typo NOT caught? (it is here, but keys widen to string)\n\n// Option B: no annotation -&gt; precise, but NOT validated\nconst b = { host: 'localhost', port: 8080 };\nb.host.toUpperCase();   // ✔ string\n// but nothing checks b against Config — a wrong value type slips through\n\n// Option C (4.9+): satisfies -&gt; BOTH\nconst c = { host: 'localhost', port: 8080 } satisfies Config;\nc.host.toUpperCase();   // ✔ string  — literal types kept\nc.port.toFixed();       // ✔ number\n// and it IS checked against Config</code></pre><h4>The rule</h4><p><code class=\"inline\">satisfies</code> <strong>checks</strong> the value against the type but keeps the <strong>inferred</strong> type as the variable's type. An annotation does the opposite: it checks, then <em>replaces</em> the type with the annotation, widening everything.</p><h4>The example that sells it</h4><pre><code>type Routes = Record&lt;string, { path: string; roles: string[] }&gt;;\n\nconst routes = {\n  home:  { path: '/',      roles: ['user'] },\n  admin: { path: '/admin', roles: ['admin'] }\n} satisfies Routes;\n\nroutes.home;      // ✔ known key — autocomplete works\nroutes.hom;       // ✘ Property 'hom' does not exist   ← annotation would ALLOW this\ntype Key = keyof typeof routes;   // 'home' | 'admin'  ← annotation gives `string`</code></pre><p>With <code class=\"inline\">const routes: Routes</code>, the key type becomes <code class=\"inline\">string</code> — so typos compile and <code class=\"inline\">keyof</code> is useless. <code class=\"inline\">satisfies</code> keeps the exact keys <em>and</em> validates the shape.</p><h4>satisfies vs as vs annotation</h4><table><thead><tr><th></th><th>Checks the value?</th><th>Keeps narrow inference?</th><th>Can lie?</th></tr></thead><tbody><tr><td><code class=\"inline\">const x: T = v</code></td><td>✔</td><td><strong>✘</strong> (widens to T)</td><td>✘</td></tr><tr><td><code class=\"inline\">const x = v as T</code></td><td>✘ (asserts)</td><td>✘</td><td><strong>✔ unsafe</strong></td></tr><tr><td><code class=\"inline\">const x = v satisfies T</code></td><td><strong>✔</strong></td><td><strong>✔</strong></td><td>✘</td></tr></tbody></table><p>Key distinction: <code class=\"inline\">as</code> is an <em>assertion</em> — you're overriding the compiler and it can be wrong at runtime. <code class=\"inline\">satisfies</code> is a <em>check</em> — it can only ever reject, never lie.</p><h4>Combining with as const</h4><pre><code>const config = { method: 'GET', retries: 3 } as const satisfies RequestConfig;\n// as const  -> literal + readonly\n// satisfies -> validated against RequestConfig</code></pre><h4>How to say it in an interview</h4><p>“Before 4.9 you chose between validating a config object and keeping its precise inferred types — annotating widens everything, so <code class=\"inline\">keyof typeof</code> gives you <code class=\"inline\">string</code> and typos compile. <code class=\"inline\">satisfies</code> checks the value against the type but leaves the inferred type in place, so you get both. And unlike <code class=\"inline\">as</code> it can't lie — it's a check, not an assertion.”</p>"
  },
  {
    "id": "ts-template-literal-type",
    "category": "typescript",
    "difficulty": "advanced",
    "tags": [
      "template-literal-types"
    ],
    "question": "What are template literal types?",
    "answer": "<p>Types built from string literals using template syntax, enabling precise string unions derived from other types — e.g. event names or CSS units.</p>",
    "tip": "Great for typing things like `on${Capitalize<Event>}` handler names.",
    "code": "type Size = 'sm' | 'lg';\ntype Class = `btn-${Size}`; // 'btn-sm' | 'btn-lg'",
    "lang": "ts"
  },
  {
    "id": "ts-generic-constraint",
    "category": "typescript",
    "difficulty": "intermediate",
    "tags": [
      "generics",
      "constraints",
      "keyof"
    ],
    "question": "How do you constrain generics (extends, keyof)?",
    "answer": "<p>Use <code class=\"inline\">extends</code> to require a shape, and <code class=\"inline\">keyof</code> to restrict a parameter to an object's keys — enabling type-safe property access helpers.</p>",
    "tip": "The typed getProp helper is a classic generics + keyof interview example.",
    "code": "function getProp<T, K extends keyof T>(obj: T, key: K): T[K] {\n  return obj[key];\n}\ngetProp({ a: 1 }, 'a'); // number",
    "lang": "ts"
  }
]
